# Rate Limiting Configuration
# OpenLiga API limit: 1000 requests/hour per IP (~0.28 RPS)
# Default: 0.25 RPS (900 requests/hour) to stay safely under the limit
RATE_LIMIT_RPS=0.25
RATE_LIMIT_BURST=5

# Exponential Backoff Configuration
MAX_RETRIES=3
BACKOFF_BASE_SECONDS=1.0
BACKOFF_MAX_SECONDS=30.0
JITTER_ENABLED=true

# OpenLiga API Configuration
OPENLIGA_BASE_URL=https://api.openligadb.de
REQUEST_TIMEOUT_SECONDS=10

# Provider Selection
# Options: openliga (default)
# To add more providers, implement SportsProvider interface and update app/providers/__init__.py
PROVIDER=openliga
